#2 SOCKETIO

이번에는 아주 쉽게 실시간 기능을 만들어주는 framework!
->socket IO : 실시간, 양방향, event 기반의 통신을 가능하게 함
->프론트와 벡엔드 간 실시간 통신을 가능하게 해주는 프레임워크 또는 라이브러리이다.
->모든 platform, 모든 브라우저, 디바이스에 이용 가능하고, 신뢰성과 속도를 제공
  != webSocket : webSocket는 socket io 방식 중 하나
  socket IO는 websocket가 지원되지 않는 경우 다른 것을 사용한다.

